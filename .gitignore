# macOS / editors
.DS_Store
.Rhistory
.Rapp.history
.Rproj.user/
.claude/
.codex_backups/
.codex_work/
.qodo/
*.Rproj
*.docx

# Secrets / local env
.env
.env.local
.env.save
.Renviron

# Runtime / generated artifacts
cache/
logs/
outputs/
/output/
/artifacts/
reviewer-results/
tmp/
_incoming_*/

# Generated indexes, derived tables, and local packaging payloads.
# These are rebuilt from the lightweight registries and external datasets.
data/
desktop/oracle-upload-*/
faToTwoBit

# Large biological datasets live outside Git history.
# Keep only lightweight documentation and registries versioned.
annotations/*
!annotations/README.md
!annotations/registry.tsv

genomes/*
!genomes/README.md
!genomes/registry.tsv

go_annotations/*
!go_annotations/README.md
!go_annotations/registry.tsv

go_annotations/raw/*

# Local download scratch space
ncbi_downloads/

# Generated SQLite alias index databases (large, rebuilt at startup)
data/alias_index/*.sqlite
data/alias_index/*.sqlite-*

# Desktop Electron app
desktop/node_modules/
desktop/dist/
desktop/resources/r/
desktop/resources/bin/
desktop/dataset-packages/work/
desktop/dataset-packages/*.zip
desktop/dataset-packages/*.dmg
desktop/dataset-packages/*.pkg
desktop/dataset-packages/*.AppImage
desktop/dataset-packages/*.exe

# Keep lightweight desktop package metadata versioned.
!desktop/dataset-packages/*.manifest.json
!desktop/dataset-packages/catalog.json

# Local CGV Desktop release installers
/INSTALABLES-FINALES/

# Build source data (large intermediate files)
build_sources/
packages/

# Screencast videos
www/screencasts/
www/screencasts.orig/
www/ctv_backup/

# Private manuscript materials (never publish in the source repository)
paper/
examples/manuscript-cases/
scripts/validate_reviewer_package.py
.github/workflows/reviewer-package.yml

/.cgv-compiled/
